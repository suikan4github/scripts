# script
My Personal Scripts

## Overview

```
.
├── configure-github-global     : Set git user.name user.email as global for GitHub
├── git
│   ├── configure-github-local  : Set git user.name user.email as directory for GitHub
│   └── configure-osdn-local    : Set git user.name user.email as direcotry for OSDN
├── kerm
│   ├── kermit_stlink           : Sub-script of ./terminal
│   └── kermit_stlinkv2         : Sub-script of ./terminal
├── README.md                   : This file
└── terminal                    : ST-Link terminal application

```